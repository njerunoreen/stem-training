from tkinter import*
root = Tk()

#create a frame 

my_label = Label (root,text ="hello world ")
#show it on screen
my_label . pack()
root. mainloop()

from tkinter import*
root = Tk()

#create a frame 


my_label = Label (root,text ="hello world ")
#show it on screen
my_label . grid (row= 0 ,column = 0)
root. mainloop()